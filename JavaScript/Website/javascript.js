
//debugger;


function mySpecialFunction(name, age){

    return "hello " + name +" I'ma " + age;
}

var result = mySpecialFunction("Jack", 36);

var myObject = {
    name: "jack",
    age: 44
 };

one ();
 function one () {
     alert ("Hello");
     
 }

 function two () {
     return 9;
 }
 console.log(5+ two());

 function summa (a,b){
     return (a+b);
 }
 console.log (summa (4,5));

 function summa (a,b){
     a = a || 10;
     b = b || 20;
     return (a + b);
    
 }
 console.log (summa (3));

 

 let t = 8;

 function showTemp (){
     if (t>0)
     console.log ("выше нуля");
     else if (t < 0)
     console.log ("ниже нуля");
     else
     console.log ("равна нулю");
 }
 showTemp();
 

 function summa1 (a,b) {
     console.log (a + b);
 }
 let c = 10;
 summa1 (17, c);

 let myMoney = 150;
 function buyFlowers () {
     if (myMoney < 150) console.log ("купить ромашки");
     else if (myMoney > 150) console.log ("купить розы");
     else console.log ("купить хризантемы");
 }
 
 
 /*

  - создать функцию
  - вернуть занчение из функции
  - передать параметр в функцию
  - создать обект со свойствами
  - поменять свойства существующего обекта
  - передать обект в функцию и внутри функции поменять некоторые свойства
  - создать новый файл и подключить
 */
